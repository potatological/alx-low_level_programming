#include <stdio.h>

/**
 * main - the entry point
 *        prints the single digit numbers of base 10
 *
 * Return: Always 0.
 */
int main(void)
{
int a = 0;
while (a < 10)
{
printf("The single digit numbers are: %d", a);
a++;
}
printf("\n");
return (0);
}
