#include <stdio.h>

/**
 * main - the entry point 
 *        program the prints lowercase alphabets
 *
 * Return: Always 0.
 */
int main(void)
{
char c = a;
while (c <= z)
{
putchar("%c", c);
c++;
}
return (0);
}
