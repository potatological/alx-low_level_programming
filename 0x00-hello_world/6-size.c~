#include <stdio.h>
/**
 * main - prints the size of various types
 *
 * Return - Always 0 (successe)
 */
int main (void)
{
  char sizeof(char);
  int sizeof(int);
  float sizeof(float);
  double sizeof(double);
}
