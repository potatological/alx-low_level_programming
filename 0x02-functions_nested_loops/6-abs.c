#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @a: the integer to have the absolute value of
 */

int _abs(int)
{
int a;
if (a >= 0)
{
a = a;
printf("%d\n", a);
}
else
{
a = -a;
printf("%d\n", a);
}
}
